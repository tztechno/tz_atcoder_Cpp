   cout << (regex_search(s,regex(".*ab|ba.*")) ? "Yes":"No") << endl;
    }

/* これは
    regex_search(s, regex(".*ab|ba.*")) checks if the regular expression ".ab|ba." matches any part of the string s. 
   コメントアウトです */

/* これは
   コメントアウトです */
